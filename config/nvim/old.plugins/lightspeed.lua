
-- =============================================================================
-- Lightspeed:
-- =============================================================================
require'lightspeed'.setup {
  ignore_case = true,
  safe_labels = {},
  jump_to_unique_chars = false,
  labels = {
    "1", "2", "3", "4", "5", "6", "7", "8", "9", "0", "-", "=",
    "!", "@", "#", "$", "%", "^", "&", "*", "(", ")",      "+"
  }
}
-- =============================================================================
