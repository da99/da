
Setup & Test
============

```bash
  bun install
  bin/__ serve test
`
Go to: http://localhost:4567


HTML
====

```javascript
  on('login', 'ok', function (e, data) { "code"; })
  on('login', 'invalid', function (e, data) { "code"; })
`
